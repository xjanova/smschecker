name: Build Release APK

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Version name (e.g. 1.0.0)'
        required: false
        default: '1.0.0'

jobs:
  release:
    name: Build Signed Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK components
        run: sdkmanager "platforms;android-34" "build-tools;34.0.0"

      - name: Grant execute permission for gradlew
        run: chmod +x android-app/gradlew

      - name: Decode keystore
        run: |
          echo "${{ secrets.SIGNING_KEYSTORE_BASE64 }}" | base64 -d > android-app/app/release.keystore
          echo "Keystore decoded: $(wc -c < android-app/app/release.keystore) bytes"

      - name: Build release APK
        id: build
        continue-on-error: true
        working-directory: android-app
        run: ./gradlew assembleRelease --stacktrace 2>&1 | tee $GITHUB_WORKSPACE/build-output.log
        env:
          SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_STORE_FILE: app/release.keystore

      - name: Append build info to log
        if: always()
        run: |
          echo "" >> build-output.log
          echo "=== BUILD OUTCOME: ${{ steps.build.outcome }} ===" >> build-output.log
          echo "=== APK FILES ===" >> build-output.log
          find android-app/app/build/outputs -name "*.apk" -type f 2>/dev/null >> build-output.log || echo "No APK found" >> build-output.log

      - name: Upload build log (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build-output.log
          retention-days: 7

      - name: Upload release APK
        if: steps.build.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: smschecker-release-${{ github.event.inputs.version_name || '1.0.0' }}
          path: android-app/app/build/outputs/apk/release/
          if-no-files-found: warn
          retention-days: 90

      - name: Fail if build failed
        if: steps.build.outcome != 'success'
        run: exit 1
